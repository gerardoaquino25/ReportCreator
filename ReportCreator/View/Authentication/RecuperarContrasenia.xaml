<UserControl x:Class="ReportCreator.View.Authentication.RecuperarContrasenia"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             Height="Auto" Width="Auto">
    <Grid>
        <StackPanel HorizontalAlignment="Left">
            <Label HorizontalAlignment="Left">
                Usuario
            </Label>
            <TextBox Name="Username" Width="200" HorizontalAlignment="Left"/>
            <DockPanel>
                <DockPanel.Resources>
                    <Style TargetType="{x:Type Button}">
                        <Setter Property="Width" Value="100"/>
                        <Setter Property="Margin" Value="20,5,20,5"/>
                    </Style>
                </DockPanel.Resources>
                <Button Content="Recuperar" Click="RecuperarClick"/>
                <Button Content="Volver" Click="VolverClick"/>
            </DockPanel>
        </StackPanel>
    </Grid>
</UserControl>
